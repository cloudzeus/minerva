# Database
# Add ?connect_timeout=3 to fail fast when DB is unreachable (e.g. local dev without DB)
DATABASE_URL="mysql://user:password@localhost:3306/minerva_rbac"

# Auth.js v5
AUTH_SECRET="your-super-secret-key-change-this-in-production"
AUTH_URL="http://localhost:3000"

# Application Base URL (for webhook callbacks)
APP_BASE_URL="http://localhost:3000"
# In production: APP_BASE_URL="https://your-domain.com"

# SMTP for alert emails (e.g. Office 365) - from address is noreply@aic.gr
SMTP_SERVER=smtp.office365.com
SMTP_PORT=587
SMTP_USER=noreply@aic.gr
SMTP_PASSWORD=your-app-password
